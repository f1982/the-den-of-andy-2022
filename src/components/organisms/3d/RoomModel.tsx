/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import * as THREE from 'three';
import React, { Suspense, useRef } from 'react';
import { Html, useGLTF, useProgress } from '@react-three/drei';
import { GLTF } from 'three-stdlib/loaders/GLTFLoader';
import { MeshProps, useFrame } from 'react-three-fiber';
import ElasticPresentation from './ElasticPresentation';

type GLTFResult = GLTF & {
  nodes: {
    mesh_all1_Texture1_0: THREE.Mesh;
  };
  materials: {};
};

// const file = '/static/models/viking_room/scene.gltf';
const file = 'https://raw.githubusercontent.com/f1982/planet-of-images/main/model/viking_room/scene.gltf';

function RoomModel(props) {
  const ref = useRef<MeshProps>();
  const { nodes, materials } = useGLTF(file) as GLTFResult;

  // useFrame((state) => {
  //   const t = state.clock.getElapsedTime();
  //   ref.current.rotation.x = -Math.PI / 1.75 + Math.cos(t / 4) / 8;
  //   ref.current.rotation.y = Math.sin(t / 4) / 8;
  //   ref.current.rotation.z = (1 + Math.sin(t / 1.5)) / 20;
  //   ref.current.position.y = (1 + Math.sin(t / 1.5)) / 10;
  // });

  return (
    <group ref={ref} {...props} dispose={null}>
      <mesh
        name="Room"
        castShadow
        receiveShadow
        geometry={nodes.mesh_all1_Texture1_0.geometry}
        material={nodes.mesh_all1_Texture1_0.material}
        position={[0, -0.3, 0]}
        rotation={[0.5, 150, 0.12]}
        scale={0.11}
      />
    </group>
  );
}
useGLTF.preload(file);

export default function ModelWithContainer() {
  return (
    <div style={{ width: '100%', height: '800px' }}>
      <ElasticPresentation>
        <RoomModel />
      </ElasticPresentation>
    </div>
  );
}